export interface Favorite {
  id: number;
  name: string;
  url: string;
  icon?: string;
}

export interface SearchEngine {
  name: string;
  url: string;
}

export interface WallpaperPreset {
  name: string;
  url: string;
}
